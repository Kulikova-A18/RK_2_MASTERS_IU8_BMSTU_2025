# Документация по запросам (с примерами curl)

Этот документ описывает все доступные API-эндпоинты системы технической поддержки, параметры запросов, примеры ответов и требования аутентификации.


## Аутентификация

Все эндпоинты (кроме `/api/v1/health`) требуют аутентификации через URL-параметры `login` и `code`.

### Формат запроса:
```bash
curl -X GET "http://localhost:5000/api/v1/<endpoint>?login=<username>&code=<password>"
```

### Доступные учетные записи (из `DEFAULT_USERS`):
| Логин | Пароль | Роль | Доступные отделы |
|-------|--------|------|------------------|
| `admin` | `AbC12xYz90Kl` | `admin` | Все отделы |
| `manager_ts` | `DeF34mNo56Pq` | `manager` | Отдел технической поддержки |
| `manager_sa` | `GhI78wXy12Rt` | `manager` | Отдел системного администрирования |
| `manager_ni` | `JkL56vBn78St` | `manager` | Отдел сетевой инфраструктуры |
| `manager_is` | `MnO90pQr34Uv` | `manager` | Отдел информационной безопасности |
| `manager_di` | `EfG23aSd56Wx` | `manager` | Отдел разработки и внедрения |
| `manager_db` | `HiJ67fGh89Yz` | `manager` | Отдел баз данных |
| `manager_ct` | `KlM01jKl23Ab` | `manager` | Отдел облачных технологий |
| `manager_ma` | `OpQ45hJk67Cd` | `manager` | Отдел мониторинга и аналитики |
| `manager_l1` | `RsT89gFg01Ef` | `manager` | Сервисный деск Level 1 |
| `manager_l2` | `UvW23eDc45Gh` | `manager` | Сервисный деск Level 2 |
| `analyst_ts` | `XyZ67iOp89Ij` | `analyst` | Отдел технической поддержки |
| `analyst_sa` | `BcD01oLm23Kl` | `analyst` | Отдел системного администрирования |
| `analyst_ni` | `EfG45pQr67Mn` | `analyst` | Отдел сетевой инфраструктуры |
| `analyst_is` | `HjK89sTu01Op` | `analyst` | Отдел информационной безопасности |
| `analyst_di` | `LmN23vWx45Qr` | `analyst` | Отдел разработки и внедрения |
| `analyst_db` | `PqR67zAb89St` | `analyst` | Отдел баз данных |
| `analyst_ct` | `TuV01cDe23Uv` | `analyst` | Отдел облачных технологий |
| `analyst_ma` | `WxY45fGh67Vw` | `analyst` | Отдел мониторинга и аналитики |
| `analyst_l1` | `AbC89jKl01Xy` | `analyst` | Сервисный деск Level 1 |
| `analyst_l2` | `DeF23mNo45Za` | `analyst` | Сервисный деск Level 2 |

> **Важно**:  
> - Используйте **только GET-запросы**.  
> - Все параметры передаются в **URL-строке** (`?login=...&code=...`).  
> - Попытка использовать POST/PUT/DELETE → `405 Method Not Allowed`.  
> - Неверный логин/пароль → `401 Unauthorized`.  
> - Невалидный формат логина → `400 Bad Request`.

---

## Доступные API-эндпоинты

---

### 1. Получить профиль пользователя  
**GET** `/api/v1/profile`

#### Пример запроса:
```bash
curl -X GET "http://localhost:5000/api/v1/profile?login=manager_ts&code=DeF34mNo56Pq"
```

#### Ответ (200 OK):
```json
{
  "staff_id": 2,
  "name": "Alexey Smirnov",
  "role": "manager",
  "departments_access": ["Отдел технической поддержки"],
  "assigned_tickets_count": 15,
  "active_tickets_count": 8
}
```

#### Поля:
| Поле | Описание |
|------|----------|
| `staff_id` | ID сотрудника в БД |
| `name` | Полное имя |
| `role` | Роль (`admin`, `manager`, `analyst`) |
| `departments_access` | Список отделов, к которым у пользователя есть доступ |
| `assigned_tickets_count` | Общее количество тикетов, назначенных пользователю |
| `active_tickets_count` | Количество тикетов в статусах: Новый, В работе, Ожидает ответа |

---

### 2. Получить список отделов с метриками  
**GET** `/api/v1/departments`

#### Пример запроса:
```bash
curl -X GET "http://localhost:5000/api/v1/departments?login=manager_is&code=MnO90pQr34Uv"
```

#### Ответ (200 OK):
```json
[
  {
    "name": "Отдел технической поддержки",
    "ticket_count": 42,
    "active_tickets": 18,
    "staff_count": 5
  },
  {
    "name": "Отдел информационной безопасности",
    "ticket_count": 21,
    "active_tickets": 7,
    "staff_count": 3
  }
]
```

#### Поля:
| Поле | Описание |
|------|----------|
| `name` | Название отдела |
| `ticket_count` | Общее количество тикетов, связанных с сотрудниками отдела |
| `active_tickets` | Количество тикетов в статусах: Новый, В работе, Ожидает ответа |
| `staff_count` | Количество активных сотрудников в отделе |

> Данные фильтруются по отделам, доступным пользователю.

---

### 3. Получить список тикетов, назначенных пользователю  
**GET** `/api/v1/tickets`

#### Пример запроса:
```bash
curl -X GET "http://localhost:5000/api/v1/tickets?login=analyst_ts&code=XyZ67iOp89Ij"
```

#### Ответ (200 OK):
```json
[
  {
    "ticket_id": 101,
    "subject": "Проблема с доступом к корпоративному порталу - 101",
    "description": "Пользователь сообщает о проблеме с доступом...",
    "created_at": "2025-04-15T10:30:00",
    "updated_at": "2025-04-16T14:20:00",
    "closed_at": null,
    "user_id": 45,
    "assigned_staff_id": 2,
    "status_id": 2,
    "category_id": 1,
    "status_name": "В работе",
    "category_name": "Проблема с входом в систему",
    "user_name": "Иванов Александр Сергеевич",
    "comments_count": 3
  }
]
```

#### Поля:
| Поле | Описание |
|------|----------|
| Все поля из таблицы `Tickets` + |
| `status_name` | Человеко-читаемое название статуса |
| `category_name` | Название категории проблемы |
| `user_name` | Полное имя пользователя, создавшего тикет |
| `comments_count` | Количество комментариев к тикету |

---

### 4. Получить детальную информацию о тикете  
**GET** `/api/v1/tickets/<ticket_id>`

#### Пример запроса:
```bash
curl -X GET "http://localhost:5000/api/v1/tickets/101?login=manager_ts&code=DeF34mNo56Pq"
```

#### Ответ (200 OK):
```json
{
  "ticket_id": 101,
  "subject": "Проблема с доступом к корпоративному порталу - 101",
  "description": "Пользователь сообщает о проблеме с доступом...",
  "created_at": "2025-04-15T10:30:00",
  "updated_at": "2025-04-16T14:20:00",
  "closed_at": null,
  "user_id": 45,
  "assigned_staff_id": 2,
  "status_id": 2,
  "category_id": 1,
  "status_name": "В работе",
  "category_name": "Проблема с входом в систему",
  "user_name": "Иванов Александр Сергеевич",
  "assigned_staff_name": "Alexey Smirnov",
  "comments": [
    {
      "comment_id": 201,
      "ticket_id": 101,
      "author_id": 45,
      "author_type": "user",
      "comment_text": "Добрый день! Проблема все еще актуальна...",
      "created_at": "2025-04-15T12:15:00",
      "author_name": "Иванов Александр Сергеевич"
    },
    {
      "comment_id": 202,
      "ticket_id": 101,
      "author_id": 2,
      "author_type": "staff",
      "comment_text": "Принял заявку в работу. Провожу диагностику...",
      "created_at": "2025-04-15T13:05:00",
      "author_name": "Alexey Smirnov"
    }
  ],
  "logs": [
    {
      "log_id": 301,
      "ticket_id": 101,
      "action": "Тикет создан в системе",
      "performed_by_staff_id": null,
      "performed_at": "2025-04-15T10:30:00"
    },
    {
      "log_id": 302,
      "ticket_id": 101,
      "action": "Тикет назначен на сотрудника 2",
      "performed_by_staff_id": 2,
      "performed_at": "2025-04-15T10:35:00"
    }
  ]
}
```

#### Особенности:
- `comments` — массив с деталями каждого комментария, включая `author_name`.
- `logs` — массив всех действий с тикетом.
- **Доступ ограничен**: пользователь может видеть только тикеты, назначенные ему.

---

### 5. Получить список сотрудников отдела  
**GET** `/api/v1/staff`

#### Пример запроса:
```bash
curl -X GET "http://localhost:5000/api/v1/staff?login=admin&code=AbC12xYz90Kl"
```

#### Ответ (200 OK):
```json
[
  {
    "staff_id": 2,
    "username": "support2",
    "full_name": "Alexey Smirnov",
    "email": "support2@company.com",
    "department": "Отдел технической поддержки",
    "position": "Специалист технической поддержки",
    "is_active": true,
    "assigned_tickets": 15,
    "active_tickets": 8,
    "resolved_tickets": 7
  },
  {
    "staff_id": 3,
    "username": "support3",
    "full_name": "Irina Kozlova",
    "email": "support3@company.com",
    "department": "Отдел системного администрирования",
    "position": "Системный администратор",
    "is_active": true,
    "assigned_tickets": 12,
    "active_tickets": 5,
    "resolved_tickets": 7
  }
]
```

#### Поля:
| Поле | Описание |
|------|----------|
| Все поля из таблицы `Staff` + |
| `assigned_tickets` | Общее количество тикетов, назначенных сотруднику |
| `active_tickets` | Количество активных тикетов (статусы 1–3) |
| `resolved_tickets` | Количество решённых тикетов (статусы 4–5) |

> Только **активные** сотрудники из **доступных** пользователю отделов.

---

### 6. Получить персональные и отделные метрики  
**GET** `/api/v1/metrics`

#### Пример запроса:
```bash
curl -X GET "http://localhost:5000/api/v1/metrics?login=analyst_sa&code=BcD01oLm23Kl"
```

#### Ответ (200 OK):
```json
{
  "personal_metrics": {
    "total_tickets": 18,
    "resolved_tickets": 12,
    "active_tickets": 5,
    "resolution_rate": "66.7%",
    "avg_resolution_time": "3.2 hours",
    "satisfaction_rate": "92%"
  },
  "department_metrics": {
    "total_tickets": 89,
    "resolved_tickets": 61,
    "avg_first_response_time": "2.1 hours",
    "most_common_category": "Проблема с входом в систему"
  }
}
```

#### Поля:
| Поле | Описание |
|------|----------|
| `personal_metrics` | Метрики для текущего пользователя |
| `department_metrics` | Метрики для всех сотрудников в доступных отделах |
| `resolution_rate` | Процент решённых тикетов от общего числа |
| `avg_resolution_time` | Среднее время решения в часах |
| `satisfaction_rate` | Случайная оценка удовлетворённости (85–98%) |
| `avg_first_response_time` | Среднее время первого ответа (заглушка) |
| `most_common_category` | Наиболее частая категория тикетов в отделе |

---

### 7. Получить временной ряд тикетов за N дней  
**GET** `/api/v1/timeline?days=<N>`

#### Пример запроса:
```bash
curl -X GET "http://localhost:5000/api/v1/timeline?login=manager_l1&code=RsT89gFg01Ef&days=7"
```

#### Ответ (200 OK):
```json
{
  "period_days": 7,
  "data": [
    {
      "date": "2025-04-09",
      "tickets_created": 2,
      "tickets_resolved": 1,
      "satisfaction_rate": 91
    },
    {
      "date": "2025-04-10",
      "tickets_created": 3,
      "tickets_resolved": 2,
      "satisfaction_rate": 89
    }
  ]
}
```

#### Параметры:
| Параметр | Значение по умолчанию | Ограничения |
|----------|------------------------|-------------|
| `days` | `30` | От `1` до `365` |

> `satisfaction_rate` — случайное значение (85–98%).

---

### 8. Сравнение производительности с отделом  
**GET** `/api/v1/comparison`

#### Пример запроса:
```bash
curl -X GET "http://localhost:5000/api/v1/comparison?login=analyst_di&code=LmN23vWx45Qr"
```

#### Ответ (200 OK):
```json
{
  "your_performance": {
    "resolution_rate": "88.5%",
    "avg_response_time": "1.8 hours",
    "satisfaction_rate": "95%"
  },
  "department_average": {
    "resolution_rate": "76.2%",
    "avg_response_time": "2.5 hours",
    "satisfaction_rate": "89%"
  },
  "top_performer": {
    "staff_name": "Ivan Petrov",
    "resolution_rate": "95.2%",
    "avg_response_time": "1.2 hours"
  }
}
```

> Все поля, кроме `resolution_rate`, — заглушки для демонстрации.

---

### 9. 🔮 Прогноз и анализ трендов  
**GET** `/api/v1/forecast`

#### Пример запроса:
```bash
curl -X GET "http://localhost:5000/api/v1/forecast?login=admin&code=AbC12xYz90Kl"
```

#### Ответ (200 OK):
```json
{
  "next_week_forecast": {
    "expected_tickets": 22,
    "expected_resolution_rate": "82%",
    "busiest_day": "Tuesday"
  },
  "trend_analysis": {
    "ticket_growth": "+12%",
    "resolution_trend": "improvement",
    "risk_factors": ["Seasonal load", "System updates"]
  }
}
```


### 10. Статистика по категориям проблем  
**GET** `/api/v1/categories`

#### Пример запроса:
```bash
curl -X GET "http://localhost:5000/api/v1/categories?login=analyst_ma&code=WxY45fGh67Vw"
```

#### Ответ (200 OK):
```json
[
  {
    "category_id": 1,
    "category_name": "Проблема с входом в систему",
    "ticket_count": 15,
    "resolution_rate": "73.3%"
  },
  {
    "category_id": 2,
    "category_name": "Ошибка в отчете",
    "ticket_count": 8,
    "resolution_rate": "87.5%"
  }
]
```

#### Поля:
| Поле | Описание |
|------|----------|
| `category_id` | ID категории |
| `category_name` | Название категории |
| `ticket_count` | Количество тикетов в этой категории |
| `resolution_rate` | Процент решённых тикетов в категории |

### 11. Проверка работоспособности сервера (без аутентификации)  
**GET** `/api/v1/health`

#### Пример запроса:
```bash
curl -X GET "http://localhost:5000/api/v1/health"
```

#### Ответ (200 OK):
```json
{
  "status": "healthy",
  "timestamp": "2025-04-15T16:45:00Z",
  "version": "1.0.0",
  "data_counts": {
    "users": 200,
    "staff": 20,
    "tickets": 200,
    "comments": 450,
    "logs": 620
  }
}
```

## Ошибки

| Код | Сообщение | Причина |
|-----|-----------|---------|
| `400` | `Invalid authentication parameters` | Логин/пароль слишком длинные (>50 / >100) |
| `401` | `Invalid credentials` | Неверный логин или пароль |
| `404` | `Endpoint not found` | Несуществующий маршрут |
| `405` | `Only GET requests are allowed` | Использован POST/PUT/DELETE |
| `500` | `Internal server error` | Ошибка на стороне сервера |


> **Примечание**:  
> Данные в API симулируются из статических списков (`TEST_USERS`, `TEST_TICKETS` и т.д.), загруженных при старте.  
> В реальной системе все данные будут браться из PostgreSQL через `db_utils`.
